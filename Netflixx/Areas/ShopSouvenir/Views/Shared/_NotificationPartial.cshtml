@{
    string successMsg = TempData["success"]?.ToString();
    string errorMsg = TempData["error"]?.ToString();
}

@if (errorMsg != null)
{
    <script>
        Swal.fire({
            icon: 'error',
            title: 'Lỗi',
            text: '@Html.Raw(errorMsg)',
            confirmButtonColor: '#e50914'
        });
    </script>
}

@if (successMsg != null)
{
    <script>
        Swal.fire({
            icon: 'success',
            title: 'Thành công',
            text: '@Html.Raw(successMsg)',
            confirmButtonColor: '#e50914'
        });
    </script>
}