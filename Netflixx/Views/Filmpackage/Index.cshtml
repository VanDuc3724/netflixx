@model IEnumerable<Netflixx.Models.PackagesModel>
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CineViet - Các Gói Dịch Vụ</title>
    <link rel="stylesheet" href="/css/FPackage.css" asp-append-version="true" />
    <div style="margin-top: 1em;">
        <a asp-action="Billhistory" asp-controller="Filmpackage" class="purchase-history-btn">Lịch sử mua</a>
        <a asp-action="Create" asp-controller="Filmpackage" class="create-package-btn">Tạo gói mới</a>
    </div>
</head>
<body>
    <main>
        <div class="page-title">
            <h1>Chọn gói phù hợp với bạn</h1>
            <p>Xem tất cả nội dung bạn yêu thích với gói giá cả phải chăng. Thay đổi hoặc hủy gói của bạn bất cứ lúc nào.</p>
        </div>

        <div class="package-container">
            @foreach (var pkg in Model)
            {
                <div class="package-card">
                    <div class="package-header">
                        <h2>@pkg.Name</h2>
                    </div>
                    @if (!string.IsNullOrEmpty(pkg.Description))
                    {
                        <div class="package-features">
                            <div class="feature">
                                <div class="feature-text">@pkg.Description</div>
                            </div>
                        </div>
                    }
                    <a class="basic-btn" asp-action="Buy" asp-route-packageId="@pkg.Id" asp-route-packageName="@pkg.Name" asp-route-packagePrice="0">Đăng ký ngay</a>
                </div>
            }
        </div>

    </main>
</body>
</html>
